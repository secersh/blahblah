{
  "name": "blahblah",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @blahblah/app",
    "build": "npm run build --workspace @blahblah/app",
    "check": "npm run check --workspace @blahblah/app"
  }
}
