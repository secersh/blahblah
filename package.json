{
  "name": "shiplog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @shiplog/app",
    "build": "npm run build --workspace @shiplog/app",
    "check": "npm run check --workspace @shiplog/app"
  }
}
